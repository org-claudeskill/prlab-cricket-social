{
  "name": "cricket-social",
  "version": "1.0.0",
  "private": true,
  "description": "Hop 3 social posts. Reads highlight clips only — three hops from cricket-protocol.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
